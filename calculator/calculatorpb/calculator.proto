syntax = "proto3";

package calculator;
option go_package="calculatorpb";

message SumRequest {
    int32 x = 1;
    int32 y = 2;
}

message SumResponse {
    int32 sum = 1;
}

service SumService {
    // Unary
    rpc Sum(SumRequest) returns (SumResponse) {}
}